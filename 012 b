Status Values
Status Value	Meaning
0	Unverified
1	Verified
2	Inactive

So in your users table

status
------
0
1
2
Laravel Implementation
1️Database Example
id	name	email	status
1	Ravi	ravi@gmail.com
	0
2	Kumar	kumar@gmail.com
	1
3	Arun	arun@gmail.com
	2
2️ Blade UI (Table)

Example:

<td>
@if($user->status == 0)
    <span class="badge bg-warning">Unverified</span>
@elseif($user->status == 1)
    <span class="badge bg-success">Verified</span>
@else
    <span class="badge bg-danger">Inactive</span>
@endif
</td>
3️ Buttons with Modal (One User Only)

Each row will have buttons.

<button class="btn btn-success verifyBtn" data-id="{{ $user->id }}">Verify</button>

<button class="btn btn-warning unverifyBtn" data-id="{{ $user->id }}">Unverify</button>

<button class="btn btn-danger inactiveBtn" data-id="{{ $user->id }}">Inactive</button>

So if you have 10 users

✔ clicking verify → only that user opens modal
✔ clicking yes → only that user status changes

4️ Single Modal (Reusable)
<div class="modal fade" id="statusModal">
<div class="modal-dialog">
<div class="modal-content">

<div class="modal-header">
<h5>Confirm Action</h5>
</div>

<div class="modal-body">
Are you sure you want to change the status?
</div>

<div class="modal-footer">
<button class="btn btn-secondary" data-bs-dismiss="modal">No</button>

<form id="statusForm" method="POST">
@csrf
<button type="submit" class="btn btn-primary">Yes</button>
</form>

</div>

</div>
</div>
</div>
5️ Controller
public function updateStatus($id,$status)
{
    $user = User::find($id);
    $user->status = $status;
    $user->save();

    return back()->with('success','Status Updated');
}
Final Flow

User Table

Name	Status	Action
Ravi	Unverified	Verify
Kumar	Verified	Inactive
Arun	Inactive	Verify

Example:

Click Verify

➡ Modal opens
➡ Yes / No

If Yes

status = 1

Only that user updates.

Best practice

0 = unverified
1 = verified
2 = inactive

This is perfect for admin panels.
