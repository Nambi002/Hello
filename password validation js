


password validation in js for my admin login / user login and register .. with toggles 

You can add JavaScript password validation + show/hide password toggle for Admin Login, User Login, and Register pages.
I will give a simple beginner-friendly code you can reuse in all pages.

️1 Password Validation Rules (Example)
Password must contain:

Minimum 8 characters

1 uppercase letter

1 lowercase letter

1 number

1 special character

Example valid password:
Admin@123

2️⃣ HTML Input with Toggle Icon
Use this for login / register password field

<div class="password-container">
    <input type="password" id="password" name="password" placeholder="Enter Password" required>

    <span class="toggle-password" onclick="togglePassword()">👁️</span>
</div>

<p id="passwordError" style="color:red;"></p>
CSS (Optional but better UI)
.password-container{
    position: relative;
    width: 300px;
}

.password-container input{
    width: 100%;
    padding: 10px;
}

.toggle-password{
    position: absolute;
    right: 10px;
    top: 10px;
    cursor: pointer;
}
3️⃣ JavaScript Password Toggle (Show / Hide)
function togglePassword() {

    let password = document.getElementById("password");

    if(password.type === "password"){
        password.type = "text";
    }else{
        password.type = "password";
    }

}
4️⃣ JavaScript Password Validation
function validatePassword(){

    let password = document.getElementById("password").value;
    let error = document.getElementById("passwordError");

    let pattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&]).{8,}$/;

    if(!pattern.test(password)){

        error.innerHTML = "Password must contain 8 characters, uppercase, lowercase, number and special character";
        return false;

    }

    error.innerHTML = "";
    return true;
}
5️⃣ Apply Validation in Form
<form onsubmit="return validatePassword()">
Example:

<form method="POST" action="/login" onsubmit="return validatePassword()">
If password is wrong
➡ form will not submit

6️⃣ Confirm Password Validation (Register Page)
Add confirm password field:

<input type="password" id="confirmPassword" placeholder="Confirm Password">

<p id="confirmError" style="color:red;"></p>
JS:

function validatePassword(){

    let password = document.getElementById("password").value;
    let confirmPassword = document.getElementById("confirmPassword").value;

    let error = document.getElementById("passwordError");
    let confirmError = document.getElementById("confirmError");

    let pattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&]).{8,}$/;

    if(!pattern.test(password)){
        error.innerHTML = "Password must contain 8 characters, uppercase, lowercase, number and special character";
        return false;
    }

    if(password !== confirmPassword){
        confirmError.innerHTML = "Passwords do not match";
        return false;
    }

    error.innerHTML = "";
    confirmError.innerHTML = "";
    return true;
}
7️⃣ Example Password Fields for Register
<input type="password" id="password" placeholder="Enter Password">

<input type="password" id="confirmPassword" placeholder="Confirm Password">
8️⃣ Where to Put JS in Laravel Blade
Inside blade file bottom:

<script>

JS CODE HERE

</script>
Or create file:

public/js/password.js
Then include:

<script src="{{ asset('js/password.js') }}"></script>
Result
Your system will have:

✔ Password validation
✔ Confirm password check
✔ Show/Hide password toggle
✔ Works for Admin Login / User Login / Register
 Since you are building Admin + User authentication system, I can also give you a very clean reusable password component (one JS for all pages) like a professional production system.

If you want, tell me. I will give that code.


not working

Since you said “not working”, I’ll give you a very simple working example step-by-step. You can copy-paste and test first, then use it in your Laravel Blade.
️1 Working HTML Form (Login / Register)
<form onsubmit="return validatePassword()">

    <div style="position:relative; width:300px;">
        <input type="password" id="password" placeholder="Enter Password" required>

        <span onclick="togglePassword()" 
        style="position:absolute; right:10px; top:8px; cursor:pointer;">
        👁
        </span>
    </div>

    <p id="error" style="color:red;"></p>

    <br>

    <button type="submit">Login</button>

</form>
2 JavaScript (Put Below Form)
<script>

function togglePassword() {

    let pass = document.getElementById("password");

    if(pass.type === "password"){
        pass.type = "text";
    }else{
        pass.type = "password";
    }

}

function validatePassword(){

    let password = document.getElementById("password").value;

    let error = document.getElementById("error");

    let pattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/;

    if(!pattern.test(password)){

        error.innerHTML = "Password must contain 8 characters, uppercase, lowercase and number";

        return false;
    }

    error.innerHTML = "";

    return true;

}

</script>
3️⃣ Test Password Examples
---> Invalid

admin
123456
password
---> Valid

Admin123
TestPass1
User12345
4--->️⃣ If Using Laravel Blade
Your password field should look like this:

<input type="password" name="password" id="password">
Your form:

<form method="POST" action="{{ route('login') }}" onsubmit="return validatePassword()">
5️⃣ Common Mistakes (Why It Doesn't Work)
Check these:
--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->--->
1--->️⃣ JS placed above HTML
➡ Put script at bottom

2--->️⃣ Wrong ID

id="password"
must match in JS:

getElementById("password")
3--->️⃣ Forgot

onsubmit="return validatePassword()"
4--->️⃣ Console error (Press F12 → Console)

65--->️⃣ If you want I can also give a Professional version with:
✔ show/hide icon (eye icon)
✔ live validation while typing
✔ strong password meter
✔ confirm password check
✔ reusable for Admin + User + Change password

This will look like a real company login system.


where want to write scripts

In Laravel Blade, you can place the JavaScript in 3 correct places. I’ll show the best and easiest way for beginners.
️⃣1---> Best Way (Write Script at Bottom of Blade File)
Example: login.blade.php or register.blade.php

<form method="POST" action="{{ route('login') }}" onsubmit="return validatePassword()">

<input type="password" id="password" name="password">

<button type="submit">Login</button>

</form>


<script>

function togglePassword(){
    let pass = document.getElementById("password");

    if(pass.type === "password"){
        pass.type = "text";
    }else{
        pass.type = "password";
    }
}

function validatePassword(){

    let password = document.getElementById("password").value;
    let pattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/;

    if(!pattern.test(password)){
        alert("Password must contain uppercase, lowercase and number with 8 characters");
        return false;
    }

    return true;
}

</script>
 Place the <script> before </body> or at the bottom of blade file.

2---️⃣ Professional Way (Separate JS File)
Create file:

public/js/password.js
Write JS there:

function validatePassword(){

    let password = document.getElementById("password").value;
    let pattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/;

    if(!pattern.test(password)){
        alert("Password must contain uppercase, lowercase and number");
        return false;
    }

    return true;
}
Then include in Blade:

<script src="{{ asset('js/password.js') }}"></script>
--->️⃣3 If You Use Layout (dashboard.app)
Then write script like this:

@section('scripts')

<script>
JS CODE HERE
</script>

@endsection
And in layout file:

@yield('scripts')
4---> Important
Your form must include:

onsubmit="return validatePassword()"
Example:

<form method="POST" action="{{ route('login') }}" onsubmit="return validatePassword()">
--->Quick Summary
Place	File
Simple	bottom of login.blade.php
Professional	public/js/password.js
Layout system	@section('scripts')
Since you are building Admin + User Login System, I can also show you how companies implement password validation + toggle eye icon + confirm password in one reusable JS file. It will reduce 80% duplicate code in your project.




