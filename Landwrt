
 Users Side : Register Page Login Page Edit Profile Page Change Password Page Admin Side : Login Page for Admin Side In the Side bar Unverified Users Verified Users Deleted/Inactive Users Here, divide the routes like Users and Admin. Both Users and Admin should have the header with logo in the Left Corner, and Logout Option in the Right Corner. Users Login shouldn't allow admin credentials same as Admin login page shouldn't allow Users credentials. Users Edit Profile, can update profile picture, name and Mob
Great task This is a very good real-time Laravel interview-level module.
I will give you a complete structured plan + folder architecture + code flow so you can finish it by end of this week.

This covers:

Routes separation (User / Admin)

Middleware (Session check)

Auth logic (separate login)

Profile edit

Change password (hashed)

Admin verification system

Active / Inactive users

Session handling

Header with logo + logout

 Overall System Architecture (Important)
Two Panels:
User Panel

Admin Panel

Core Logic:
User registers → Goes to Unverified Users

Admin verifies → User can login

Admin can deactivate → Moves to Deleted/Inactive Users

 Step 1: Laravel Project Structure
Inside your Laravel project:

app/
 ├── Http/
 │   ├── Controllers/
 │   │   ├── User/
 │   │   │    AuthController.php
 │   │   │    ProfileController.php
 │   │   ├── Admin/
 │   │        AdminAuthController.php
 │   │        UserManagementController.php
 │   ├── Middleware/
 │        UserAuthMiddleware.php
 │        AdminAuthMiddleware.php
🗄️ Step 2: Database Design (Very Important)
Create Users Table (Custom)

Migration
php artisan make:migration create_users_table
users table fields:
Schema::create('users', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('mobile')->unique();
    $table->string('email')->unique();
    $table->string('password');
    $table->string('profile_image')->nullable();
    $table->enum('role', ['user', 'admin'])->default('user');
    $table->enum('status', ['unverified', 'verified', 'inactive'])->default('unverified');
    $table->timestamps();
});
Then run:

php artisan migrate
 Step 3: Route Separation (User & Admin)
routes/web.php
use App\Http\Controllers\User\AuthController;
use App\Http\Controllers\User\ProfileController;
use App\Http\Controllers\Admin\AdminAuthController;
use App\Http\Controllers\Admin\UserManagementController;

/*
|--------------------------------------------------------------------------
| USER ROUTES
|--------------------------------------------------------------------------
*/
Route::prefix('user')->group(function () {

    Route::get('/register', [AuthController::class, 'showRegister']);
    Route::post('/register', [AuthController::class, 'register']);

    Route::get('/login', [AuthController::class, 'showLogin']);
    Route::post('/login', [AuthController::class, 'login']);

    Route::middleware(['user.auth'])->group(function () {
        Route::get('/profile', [ProfileController::class, 'editProfile']);
        Route::post('/profile/update', [ProfileController::class, 'updateProfile']);
        Route::get('/change-password', [ProfileController::class, 'changePasswordForm']);
        Route::post('/change-password', [ProfileController::class, 'changePassword']);
        Route::get('/logout', [AuthController::class, 'logout']);
    });
});

/*
|--------------------------------------------------------------------------
| ADMIN ROUTES
|--------------------------------------------------------------------------
*/
Route::prefix('admin')->group(function () {

    Route::get('/login', [AdminAuthController::class, 'showLogin']);
    Route::post('/login', [AdminAuthController::class, 'login']);

    Route::middleware(['admin.auth'])->group(function () {
        Route::get('/unverified-users', [UserManagementController::class, 'unverified']);
        Route::get('/verified-users', [UserManagementController::class, 'verified']);
        Route::get('/inactive-users', [UserManagementController::class, 'inactive']);
        Route::get('/verify/{id}', [UserManagementController::class, 'verifyUser']);
        Route::get('/inactive/{id}', [UserManagementController::class, 'inactiveUser']);
        Route::get('/logout', [AdminAuthController::class, 'logout']);
    });
});
 Step 4: Middleware (Session Token Check)
Create Middleware
php artisan make:middleware UserAuthMiddleware
php artisan make:middleware AdminAuthMiddleware
UserAuthMiddleware
public function handle($request, Closure $next)
{
    if (!session()->has('user_id')) {
        return redirect('/user/login');
    }
    return $next($request);
}
AdminAuthMiddleware
public function handle($request, Closure $next)
{
    if (!session()->has('admin_id')) {
        return redirect('/admin/login');
    }
    return $next($request);
}
Register in Kernel:

'user.auth' => \App\Http\Middleware\UserAuthMiddleware::class,
'admin.auth' => \App\Http\Middleware\AdminAuthMiddleware::class,
Step 5: User Register Logic (With Verification Status)
AuthController.php (User)
use App\Models\User;
use Illuminate\Support\Facades\Hash;

public function register(Request $request)
{
    User::create([
        'name' => $request->name,
        'email' => $request->email,
        'mobile' => $request->mobile,
        'password' => Hash::make($request->password),
        'status' => 'unverified'
    ]);

    return redirect('/user/login')->with('msg', 'Registered! Wait for admin verification.');
}
 Step 6: User Login (Prevent Admin Login)
public function login(Request $request)
{
    $user = User::where('email', $request->email)
        ->where('role', 'user')
        ->first();

    if ($user && Hash::check($request->password, $user->password)) {

        if ($user->status != 'verified') {
            return back()->with('error', 'Account not verified by admin');
        }

        session(['user_id' => $user->id]);
        return redirect('/user/profile');
    }

    return back()->with('error', 'Invalid Credentials');
}
 Step 7: Admin Login (Block Users)
public function login(Request $request)
{
    $admin = User::where('email', $request->email)
        ->where('role', 'admin')
        ->first();

    if ($admin && Hash::check($request->password, $admin->password)) {
        session(['admin_id' => $admin->id]);
        return redirect('/admin/unverified-users');
    }

    return back()->with('error', 'Admin login only');
}
 Step 8: Edit Profile (Only Name, Mobile, Profile Pic)
public function updateProfile(Request $request)
{
    $user = User::find(session('user_id'));

    if ($request->hasFile('profile_image')) {
        $file = $request->file('profile_image')->store('profiles', 'public');
        $user->profile_image = $file;
    }

    $user->name = $request->name;
    $user->mobile = $request->mobile;
    $user->save();

    return back()->with('success', 'Profile Updated');
}
 Step 9: Change Password (Secure - Interview Level)
public function changePassword(Request $request)
{
    $user = User::find(session('user_id'));

    if (!Hash::check($request->current_password, $user->password)) {
        return back()->with('error', 'Current password incorrect');
    }

    if ($request->new_password != $request->confirm_password) {
        return back()->with('error', 'Passwords do not match');
    }

    $user->password = Hash::make($request->new_password);
    $user->save();

    return back()->with('success', 'Password Changed Successfully');
}
 Step 10: Admin Panel – User Management
Unverified Users
public function unverified()
{
    $users = User::where('status', 'unverified')->get();
    return view('admin.unverified', compact('users'));
}
Verify User
public function verifyUser($id)
{
    User::where('id', $id)->update(['status' => 'verified']);
    return back();
}
Inactive User (Soft Delete Logic)
public function inactiveUser($id)
{
    User::where('id', $id)->update(['status' => 'inactive']);
    return back();
}
 Step 11: Header Layout (Common for User & Admin)
Create:

resources/views/layouts/header.blade.php
<div style="display:flex; justify-content:space-between; padding:15px; background:#eee;">
    <div>
        <img src="/logo.png" width="120">
    </div>
    <div>
        <a href="{{ url('logout') }}">Logout</a>
    </div>
</div>
Include in all pages:

@include('layouts.header')
 5-Day Completion Plan (End of Week Target)
Day 1:
Migration

Models

Route setup (User/Admin)

Day 2:
User Register & Login (Session + Hash)

Day 3:
Middleware

Profile Edit + Upload Image

Change Password

Day 4:
Admin Login

Unverified / Verified / Inactive pages

Day 5:
UI (Sidebar + Header + Logo)

Testing all flows

Bug fixing (Interview ready)

 Bonus (For Interview Impression)
Add:

Form Validation

CSRF Protection

Image validation (jpg/png only)

Session timeout auto logout

 Next (Very Important)
Do you want me to give you:

A) Full Blade UI (Register, Login, Admin Panel with Sidebar)
OR

B) Complete GitHub-level Full Project Code (Copy-Paste Ready Laravel)
Reply: A or B (I will generate full production-level code)


Y
Step by step  beginner 


Perfect 
Since you are a beginner, I will teach this Laravel task STEP BY STEP (very easy) — no confusion, no skipping.

We will build:

User Register

User Login (only verified users)

Admin Login (separate)

Profile Edit

Change Password

Admin Verification System

Middleware + Session

And finish safely before weekend.

 STEP 0: Install Laravel Project (First Time Setup)
Open Terminal (or VS Code terminal)

composer create-project laravel/laravel user-admin-system
cd user-admin-system
php artisan serve
Open in browser:

http://127.0.0.1:8000
 STEP 1: Database Setup (Very Important)
Open .env file and change DB settings:

DB_DATABASE=laravel_db
DB_USERNAME=root
DB_PASSWORD=
Now create database in phpMyAdmin:

laravel_db
 STEP 2: Create Users Table (With Role & Status)
Run:

php artisan make:migration create_users_table
Open migration file:

database/migrations/xxxx_create_users_table.php
Replace code with:

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up(): void {
        Schema::create('users', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->string('email')->unique();
            $table->string('mobile')->unique();
            $table->string('password');
            $table->string('profile_image')->nullable();
            $table->enum('role', ['user', 'admin'])->default('user');
            $table->enum('status', ['unverified', 'verified', 'inactive'])->default('unverified');
            $table->timestamps();
        });
    }

    public function down(): void {
        Schema::dropIfExists('users');
    }
};
Now migrate:

php artisan migrate
 STEP 3: Create Model (User)
Laravel already has:

app/Models/User.php
Open it and update:

protected $fillable = [
    'name',
    'email',
    'mobile',
    'password',
    'profile_image',
    'role',
    'status'
];
 STEP 4: Create Controllers (User & Admin)
Run these commands:

php artisan make:controller User/AuthController
php artisan make:controller User/ProfileController
php artisan make:controller Admin/AdminAuthController
php artisan make:controller Admin/UserManagementController
Now your structure becomes clean (interview level).

 STEP 5: Setup Routes (User & Admin Separate)
Open:

routes/web.php
Paste this:

use App\Http\Controllers\User\AuthController;
use App\Http\Controllers\User\ProfileController;
use App\Http\Controllers\Admin\AdminAuthController;
use App\Http\Controllers\Admin\UserManagementController;

/*
|---------------- USER ROUTES ----------------|
*/
Route::prefix('user')->group(function () {

    Route::get('/register', [AuthController::class, 'showRegister']);
    Route::post('/register', [AuthController::class, 'register']);

    Route::get('/login', [AuthController::class, 'showLogin']);
    Route::post('/login', [AuthController::class, 'login']);

    Route::get('/logout', [AuthController::class, 'logout']);
});

/*
|---------------- ADMIN ROUTES ----------------|
*/
Route::prefix('admin')->group(function () {

    Route::get('/login', [AdminAuthController::class, 'showLogin']);
    Route::post('/login', [AdminAuthController::class, 'login']);
});
Test URLs:

/user/register

/user/login

/admin/login

 STEP 6: Create Basic Blade Views (Beginner UI)
Create folder:

resources/views/user/
 Register Page
Create:

resources/views/user/register.blade.php
<h2>User Register</h2>

<form method="POST" action="/user/register">
    @csrf
    Name: <input type="text" name="name"><br><br>
    Email: <input type="email" name="email"><br><br>
    Mobile: <input type="text" name="mobile"><br><br>
    Password: <input type="password" name="password"><br><br>

    <button type="submit">Register</button>
</form>
 STEP 7: User Register Logic (Controller)
Open:

app/Http/Controllers/User/AuthController.php
Paste:

namespace App\Http\Controllers\User;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;
use App\Models\User;
use Illuminate\Support\Facades\Hash;

class AuthController extends Controller
{
    public function showRegister()
    {
        return view('user.register');
    }

    public function register(Request $request)
    {
        User::create([
            'name' => $request->name,
            'email' => $request->email,
            'mobile' => $request->mobile,
            'password' => Hash::make($request->password),
            'role' => 'user',
            'status' => 'unverified'
        ]);

        return redirect('/user/login')->with('success', 'Registered! Wait for admin verification.');
    }
}
Now test:

http://127.0.0.1:8000/user/register
Register a user → It will go to DB as unverified ✅

 STEP 8: Create User Login Page
Create:

resources/views/user/login.blade.php
<h2>User Login</h2>

@if(session('error'))
{{ session('error') }}
@endif

<form method="POST" action="/user/login">
    @csrf
    Email: <input type="email" name="email"><br><br>
    Password: <input type="password" name="password"><br><br>

    <button type="submit">Login</button>
</form>
 STEP 9: User Login Logic (Block Admin + Check Verified)
Update AuthController:

public function showLogin()
{
    return view('user.login');
}

public function login(Request $request)
{
    $user = User::where('email', $request->email)
                ->where('role', 'user')
                ->first();

    if (!$user) {
        return back()->with('error', 'Invalid User Credentials');
    }

    if (!Hash::check($request->password, $user->password)) {
        return back()->with('error', 'Wrong Password');
    }

    if ($user->status != 'verified') {
        return back()->with('error', 'Account not verified by Admin');
    }

    session(['user_id' => $user->id]); // SESSION START

    return redirect('/user/dashboard');
}
 What We Completed (Beginner Progress)

 NEXT STEP (Very Important for Your Task)
In Step 2 we will build:

Middleware (Session protection)

User Dashboard

Logout (Destroy Session)

Admin Login Page

Admin Verify Users Panel (Core requirement)

Before I continue, tell me:

Have you successfully completed Step 1–9?
Reply with:

“YES DONE” (or)

Error screenshot (I will fix instantly)

Then I will give STEP 10: Middleware + Session + Admin Panel (Unverified Users) 🔥



Fir admin login default admin 1234 username and password but user login i want dynamic like registered user


Got it 
You want:

 Admin Login → Default (username: admin, password: 1234)

 User Login → Dynamic (from registered users table)

This is a very good beginner approach and commonly used in tasks.

I will give you simple step-by-step implementation (beginner friendly).

 What We Will Do
Admin login will NOT use database

It will use fixed credentials (admin / 1234)

User login will use database (dynamic registered users)

STEP 1: Create Admin Login View
Create folder:

resources/views/admin/
Create file:

resources/views/admin/login.blade.php
Paste this simple code:

<h2>Admin Login</h2>

@if(session('error'))
<p style="color:red;">{{ session('error') }}</p>
@endif

<form method="POST" action="/admin/login">
    @csrf
    Username: <input type="text" name="username"><br><br>
    Password: <input type="password" name="password"><br><br>

    <button type="submit">Admin Login</button>
</form>
 STEP 2: Create AdminAuthController (If not created)
Run command:

php artisan make:controller Admin/AdminAuthController
Open:

app/Http/Controllers/Admin/AdminAuthController.php
Replace full code:

<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

class AdminAuthController extends Controller
{
    // Show admin login page
    public function showLogin()
    {
        return view('admin.login');
    }

    // Default Admin Login (Static)
    public function login(Request $request)
    {
        $username = $request->username;
        $password = $request->password;

        // Default Admin Credentials
        if ($username === 'admin' && $password === '1234') {

            // Create Admin Session
            session(['admin_logged_in' => true]);

            return redirect('/admin/dashboard');
        }

        return back()->with('error', 'Invalid Admin Credentials');
    }

    // Logout
    public function logout()
    {
        session()->forget('admin_logged_in');
        return redirect('/admin/login');
    }
}
 STEP 3: Update Routes (Very Important)
Open:

routes/web.php
Add/Update Admin routes:

use App\Http\Controllers\Admin\AdminAuthController;

/*
|---------------- ADMIN ROUTES ----------------|
*/
Route::prefix('admin')->group(function () {

    Route::get('/login', [AdminAuthController::class, 'showLogin']);
    Route::post('/login', [AdminAuthController::class, 'login']);
    Route::get('/logout', [AdminAuthController::class, 'logout']);

    // Temporary dashboard (after login)
    Route::get('/dashboard', function () {
        return "Welcome Admin Dashboard";
    });
});
Now test:

http://127.0.0.1:8000/admin/login
Login with:

Username: admin
Password: 1234
 STEP 4: User Login = Dynamic (Database Based) ✔ (Already Correct)
Your user login should stay like this (dynamic):

$user = User::where('email', $request->email)
            ->where('role', 'user')
            ->first();
Why this is correct?

Admin → Static login (admin / 1234)

User → DB registered users

Admin cannot login in user panel 

User cannot login in admin panel 

This matches your task requirement perfectly.

 STEP 5: Prevent Admin Credentials in User Login (Extra Safety)
Update your User login method:

Open:

User/AuthController.php
Inside login function add this:

if ($request->email == 'admin') {
    return back()->with('error', 'Admin cannot login from user panel');
}
Full beginner-safe logic:

public function login(Request $request)
{
    $user = User::where('email', $request->email)
                ->where('role', 'user')
                ->first();

    if (!$user) {
        return back()->with('error', 'User not found');
    }

    if (!\Hash::check($request->password, $user->password)) {
        return back()->with('error', 'Wrong Password');
    }

    if ($user->status != 'verified') {
        return back()->with('error', 'Wait for admin verification');
    }

    session(['user_id' => $user->id]);

    return redirect('/user/dashboard');
}
 Final Login Flow (As per Your Task Requirement)
 USER SIDE
Register → Stored in DB (unverified)

Login → Dynamic (DB)

Only verified users can login

 ADMIN SIDE
Login → Static (admin / 1234)

No DB needed for admin

Can verify users

Can deactivate users
